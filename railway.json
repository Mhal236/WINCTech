{
  "version": 2,
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo Building API server"
  },
  "deploy": {
    "startCommand": "node server.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 